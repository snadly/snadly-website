# Kilo website - server-side configuration.
# Copy to `.env` and fill in. Values here are read server-side only.

# Public production URL used by metadata and canonical links.
SITE_URL=https://kilo.nz

# Email address shown as the primary contact fallback until forms/content are added.
CONTACT_EMAIL=
